form(class="{{cssClass}}" autocomplete="off")
  header.sheet-header
    img(src="{{item.img}}" data-edit="img" title="{{item.name}}" height="64" width="64")
    h1
      input(input name="name" type="text" value="{{item.name}}" placeholder="Name")
    h2 THIS IS WORKING!